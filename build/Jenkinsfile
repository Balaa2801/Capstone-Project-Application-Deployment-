pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    sh 'chmod +x build/build.sh'
                    sh './build/build.sh'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_HUB_USERNAME', passwordVariable: 'DOCKER_HUB_PASSWORD')]) {
                        echo "Logging into Docker Hub"
                        sh "docker login -u balaji8122 -p $DOCKER_HUB_PASSWORD" // Replace DOCKER_HUB_USERNAME with balaji8122
                        if (env.BRANCH_NAME == 'dev') {
                            echo "Tagging and pushing Docker image to dev repository"
                            sh "docker tag my-app:latest balaji8122/dev:latest" // Replace DOCKER_HUB_USERNAME with balaji8122
                            sh "docker push balaji8122/dev:latest" // Replace DOCKER_HUB_USERNAME with balaji8122
                        } else if (env.BRANCH_NAME == 'master') {
                            echo "Tagging and pushing Docker image to prod repository"
                            sh "docker tag my-app:latest balaji8122/prod:latest" // Replace DOCKER_HUB_USERNAME with balaji8122
                            sh "docker push balaji8122/prod:latest" // Replace DOCKER_HUB_USERNAME with balaji8122
                        }
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    sh 'chmod +x build/deploy.sh'
                    if (env.BRANCH_NAME == 'dev') {
                        sh './build/deploy.sh dev'
                    } else if (env.BRANCH_NAME == 'master') {
                        sh './build/deploy.sh prod'
                    }
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}

